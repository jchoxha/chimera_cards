# Copy this file to `.env` and fill in your key(s). Used only by local
# art-generation scripts — never shipped to the browser / committed.
GEMINI_API_KEY=

# PixelLab (https://pixellab.ai) — bakes roster portraits:
#   node scripts/bake_roster_pixellab.mjs
PIXELLAB_API_KEY=
