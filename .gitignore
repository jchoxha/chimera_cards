node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.*
!.env.example

# Python tooling caches (scripts/)
__pycache__/
*.pyc

# Editor/OS
.vscode/*
!.vscode/extensions.json
.idea

# Local Claude Code settings (personal, not for the repo)
.claude/settings.local.json

# Private design reference (StS dumps etc.) — never commit this folder
AI context/
