import React from "react";
import ReactDOM from "react-dom/client";
import ChimeraCards from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ChimeraCards />
  </React.StrictMode>
);
